# 台指訊號判斷引擎（Side Project）

## 目標

一個持續運行的程式：連上報價來源，讓使用者用大小台的變數組合出判斷式，
程式即時算出判斷式結果是 `true` 或 `false`。**目前範圍不含自動下單**，
純粹是訊號判斷引擎，未來若要延伸到自動下單再另外評估。

## 需要的變數

-   大台 (TX) / 小台 (MTX)
-   a1（委賣一價）、b1（委買一價）
-   最新成交價、成交量
-   現在時間

## 資料來源

### 現階段：FinMind

-   免費、不需要開任何券商帳戶，註冊會員驗證信箱即可拿 token，額度每小時 600 次請求
-   純 REST API，Go 可以直接呼叫，不需要額外的語言橋接
-   相關資料集：`taiwan_futures_snapshot`（台股期貨即時資訊）、
    `TaiwanFutOptTickInfo`（期貨/選擇權即時報價總覽）
-   限制（先動手做，之後要實測確認）：
    -   第三方彙整資料，非券商官方直送，實際延遲多少要自己量測
    -   要確認資料集是否真的含 a1/b1 五檔報價，還是只有成交價量
    -   免費額度是否夠支撐研究階段的輪詢頻率

### 未來：券商 API（暫緩）

等訊號邏輯驗證完、需要更低延遲或要接自動下單時，再評估換成：

-   **Fugle（玉山證券富果帳戶）**：原生 REST + WebSocket，Go 可直接串接，架構最乾淨
-   **永豐金證券 Shioaji**：社群資源最多，但官方 SDK 只有 Python，需要額外的 Python adapter 橋接

這兩者都需要開對應券商戶，此步驟需使用者自行完成，暫不影響現階段開發。

## 架構

FinMind 是純 REST API，現階段用**單一 Go 程式**就能完成，不需要額外的 Python
adapter 進程：

```text
[Go 程式]
  data 層：定時呼叫 FinMind REST API，取得 TX/MTX 最新 a1/b1/price/volume/time
  engine 層：解析使用者判斷式、代入最新變數值、求值
  輸出：true/false
```

-   **data 層要抽成介面**（例如 `QuoteProvider`），輸入/輸出格式跟資料來源無關，
    這樣未來要換成 Fugle 或 Shioaji（甚至額外加一支 Python adapter）時，
    只需要新增一個實作，`engine` 層完全不用改
-   **engine 層**：判斷式解析（考慮 `expr-lang/expr` 或 `govaluate`），
    讓使用者可以寫類似 `TX.a1 > TX.b1 && TX.volume > 1000` 的條件式，
    定時向 data 層拉最新值代入求值

## 專案階段

### Phase 1 資料連線驗證

-   FinMind 註冊帳號、取得 token
-   Go 程式：呼叫 `taiwan_futures_snapshot` / `TaiwanFutOptTickInfo`，印出 TX/MTX 最新值
-   實測資料延遲、確認欄位是否含 a1/b1、確認額度是否夠用

### Phase 2 資料層抽象

-   定義 `QuoteProvider` 介面（TX/MTX 各自的 a1/b1/price/volume/time）
-   實作 FinMind 版本
-   定時輪詢機制、錯誤處理（額度超限、請求失敗、欄位缺值）

### Phase 3 判斷引擎

-   選定表達式解析套件
-   支援的判斷式語法設計（比較運算、邏輯運算、變數命名規則）
-   定時從 data 層拉最新變數、代入求值、輸出 true/false
-   基本錯誤處理：資料層無回應、變數缺值時的行為

### Phase 4 整合測試

-   端到端測試：輸入判斷式、驗證不同市場情境下輸出正確
-   量測從資料更新到判斷輸出的延遲，評估是否足以支撐後續用途

### Phase 5（暫緩）換成券商 API

-   視 Phase 1~4 驗證結果，評估是否需要換成 Fugle 或 Shioaji
-   因為 `QuoteProvider` 介面已抽象化，只需新增實作，`engine` 層不動

## 後續可擴充（暫不在目前範圍）

-   接自動下單（券商下單 API）
-   多組判斷式同時監控
-   訊號觸發後的通知（Line/Telegram/Webhook）
-   歷史 tick 紀錄供回測

## 注意事項

-   目前範圍只到「輸出 true/false」，不涉及下單，避免一開始就處理風控/資金管理的複雜度
-   `QuoteProvider` 介面要設計清楚，之後才能在不動判斷引擎邏輯的情況下更換資料來源或加下單功能
-   FinMind 資料的即時性/完整性尚未實測驗證，Phase 1 要優先確認，若不符需求再提前評估換券商 API
